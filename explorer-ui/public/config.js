// Runtime configuration, replaced on container startup from PREIS_URL by
// docker-entrypoint.d/40-runtime-config.sh. This checked-in copy is what `npm run
// dev` and an unconfigured container serve: no values, so the app falls back to
// its build-time or local defaults.
window.__NECKWORK_CONFIG__ = {}
